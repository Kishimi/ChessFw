#include <iostream>

// #include <ChessFw/ChessFw.hpp>

int main()
{
	/*
	ChessFw::Board board = ChessFw::Board::Standard();
	// ChessFw::Engine<> engine;

	while (!board.CeckMate(ChessFw::Piece))
	{
		if (board.ColorToMove() == ChessFw::Piece::White)
			std::cout << "White: ";
		else if (board.ColorToMove() == ChessFw::Piece::Black)
			std::cout << "Black: ";

		std::string move;
		std::getline(std::cin, move);

		board.HalfMove(move);
		// board.Move(move, engine.GetMove());
	}
	*/

	return 0;
}